(function(){
    angular.module('ngPharm.services')
        .service('PriceService', ['$http', function($http) {
            return {
                lookup: function(name) {
                    
                }
            }
        });
})();
